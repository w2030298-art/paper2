algorithm:
  batch_size: 512
  gamma: 0.99
  name: MADDPG
  tau: 0.005
evaluation:
  num_episodes: 10
game_theory:
  cpnet_enabled: true
  ctde_with_hints: true
  efx_enabled: true
  efx_transfer_rate: 0.5
  enabled: true
  reward_weights:
  - 0.8
  - 0.1
  - 0.1
  use_shapley_credit: true
  warm_start_lr_scale: 0.4
  warm_start_steps: 2000
logging:
  checkpoint_dir: checkpoints/maddpg
  eval_interval: 2500
  log_interval: 10
  save_interval: 50000
  min_eval_points: 32
  eval_at_start: true
network:
  hidden_dim: 256
training:
  lr: 0.0002
  rollout_steps: 2048
  seed: 42
  total_timesteps: 100000
