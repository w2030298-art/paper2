# docs/inbox

This slim package is meant to replace `docs/` directly.

For future Iter patches, place the next full replacement `plan.md` in `docs/inbox/plan.md` and let the execution side merge it back.
